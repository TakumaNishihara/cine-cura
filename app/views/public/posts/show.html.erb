<div class="row">
  <div class="col-12 col-lg-6">
    <h2><span class="px-3 font-weight-bold" style="background-color: #e8e8e8;">投稿詳細</span></h2>

    <%= image_tag @post.get_image(1000, 1000), style: "width:70%;" %>
    <table class="table mt-4">
      <tbody>
        <tr>
          <th width="23%">タイトル</th>
          <td><%= @post.title %></td>
        </tr>
        <tr>
          <th>本文</th>
          <td><%= @post.body %></td>
        </tr>
        <tr>
          <th>公開年</th>
          <td><%= @post.release %></td>
        </tr>
        <tr>
          <th>シリーズ順番</th>
          <td><%= @post.timeline %></td>
        </tr>
        <tr>
          <th class="align-middle">投稿者</th>
          <td>
            <%= link_to( admin_signed_in? ? edit_admin_user_path(@post.user) : user_path(@post.user) ) do %>
              <%= image_tag @post.user.get_profile_image(50, 50), class: 'rounded-circle' %>
              <%= @post.user.name %>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>

  <div class="d-flex justify-content-end">
    <% if @post.user == current_user || admin_signed_in? %>
      <%= link_to "編集", edit_post_path(@post.id), class: "btn btn-secondary m-1 px-4" %>
      <%= link_to "削除", post_path(@post), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger m-1 px-4" %>
    <% end %>
  </div>

    <div class="d-none d-lg-block">
      <%= render "public/comments/form", post: @post, comment: @comment %>
    </div>
  </div>
  <div class="col-12 col-lg-6">
    <h2>コメント一覧</h2>

    <%= render "public/comments/index", post: @post %>
    <div class="d-block d-lg-none">
      <%= render "public/comments/form", post: @post, comment: @comment %>
    </div>
  </div>
</div>

