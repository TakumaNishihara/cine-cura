<h1>投稿一覧</h1>
<table>
  <thead>
    <tr>
      <th>画像</th>
      <th>タイトル</th>
      <th>公開年</th>
      <th>時系列</th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td>
          <%= image_tag post.get_image(70, 70) %>
        </td>
        <td>
          <%= link_to post.title, post_path(post.id) %>
        </td>
        <td>
          <%= post.release %>
        </td>
        <td>
          <%= post.timeline %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>