<h1>投稿一覧</h1>
<table>
  <thead>
    <tr>
      <th>画像</th>
      <th>タイトル</th>
      <th>リリース</th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td>
          <%= image_tag post.get_image, size: "50x50" %>
        </td>
        <td>
          <%= link_to post.title, post_path(post.id) %>
        </td>
        <td>
          <%= post.release %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>