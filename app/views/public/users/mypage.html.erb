マイページ
<div>
  <h3><%= @user.name %></h3>
  <%#= image_tag @user.get_profile_image(100,100) %>
  <%= image_tag @user.get_profile_image, size:'100x100' %>
  <p><%= link_to "プロフィール編集", edit_user_path(current_user), class: 'btn btn-secondary' %></p>
</div>

<h2>あなたの投稿一覧</h2>
<%= render 'public/posts/index', posts: @posts %>

<%#= paginate @posts %>